server:
  port: 9005
spring:
  application:
    name: fantasy-oss
  cloud:
    nacos:
      config:
        shared-configs:
          - data-id: mysql-alone.yaml
            refresh: true
          - data-id: mybatis-plus.yaml
            refresh: true
          - data-id: mybatis-plus-base-meta.yaml
            refresh: true
          - data-id: pagehelper.yaml
            refresh: true
          - data-id: repeat-submit.yaml
            refresh: true
          - data-id: redis-alone.yaml
            refresh: true
          - data-id: notify-mail.yaml
            refresh: true
          - data-id: swagger-knife.yaml
            refresh: true
  profiles:
    active: 'product'