server:
  port: 9020
spring:
  application:
    name: fantasy-chess
  cloud:
    nacos:
      config:
        shared-configs:
          - data-id: mysql-alone.yaml
            refresh: true
          - data-id: mybatis-plus.yaml
            refresh: true
          - data-id: mybatis-plus-base-meta.yaml
            refresh: true
          - data-id: pagehelper.yaml
            refresh: true
          - data-id: redis-alone.yaml
            refresh: true
          - data-id: repeat-submit.yaml
            refresh: true
  #          - data-id: swagger-knife.yaml
  #            refresh: true
  profiles:
    active: 'product'